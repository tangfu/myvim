【myvim】
我的vim设置


【安装】
git clone git://github.com/tangfu/myvim.git
cd myvim
./install_myvim.sh


【Change】

tabbar:
# 修改映射
原始映射：ESC + NUM
修改后	：NUM(正常模式下)，ESC + NUM(插入模式下)


snipmate:
# 添加switch 
snippet sw
	switch {
		case ${1:condition1}:
			break;
		default:
			break;
	}
# 添加case
snippet case
	case ${1:condition1}:
		break;
